<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.kangaroonew">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:usesCleartextTraffic="true">
        <activity android:name=".insideInbox.DosageDetails">
        </activity>
        <activity android:name=".insideInbox.MyHeathDetails">
        </activity>
        <activity android:name=".insideInbox.AppointmentDetails">
        </activity>

        <service
                android:name=".AppointmentStatus"
                android:exported="false">
        </service>

        <activity android:name=".Appointment">
        </activity>
        <activity android:name=".Inbox">
        </activity>
        <activity android:name=".Feedback">
        </activity>
        <activity android:name=".Home">
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>